class NegotiationController {

    add(event) {
        event.preventDefault();

        let $ = document.querySelector.bind(document);
        let inputDate = document.$('#date');
        let inputQuantity = document.$('#quantity');
        let inputValue = document.$('#value');
    }
}